trigger:
- master

resources:
  repositories:
  - repository: templates
    type: github
    name: JeremyTCD/DevOps.AzurePipelines.Nuget
    endpoint: JeremyTCD

jobs:
- template: main.yml@templates
  parameters:
    nugetRestoreEndPoint: "https://pkgs.dev.azure.com/JeremyTCD/_packaging/Main/nuget/v2"
    nugetPushEndpoint: "https://staging.nuget.org/api/v2/package"