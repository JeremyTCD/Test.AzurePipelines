trigger:
- master

variables:
  NUGET_PACKAGES: $(System.DefaultWorkingDirectory)/packages # TODO necessary even with CopyLocalLockFileAssemblies?
  DOTNET_SKIP_FIRST_TIME_EXPERIENCE: true

resources:
  repositories:
  - repository: templates
    type: github
    name: JeremyTCD/DevOps.AzurePipelines
    endpoint: JeremyTCD

jobs:
- template: nuget-package.yml@templates
